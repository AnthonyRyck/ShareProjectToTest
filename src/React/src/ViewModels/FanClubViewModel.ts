import FakeAccessData from "../data/FakeAccessData";


export default class FanClubViewModel {

	//private readonly FakeData: FakeAccessData;

	// constructor(data: FakeAccessData)
	// {
	// 	this.FakeData = data;
	// }

	// @action onCounterClick = (id: number) : void =>
	// {
	// 	this.FakeData.AddClick(id);
	// };

//   @observable private count = 0;
//   private document: Document;

//   constructor(document: Document) {
//     this.document = document;
//     this.document.title = `You clicked ${this.count} times`;
//   }

//   @action onClick = (): void => {
//     this.count += 1;
//     this.document.title = `You clicked ${this.count} times`;
//   };

//   @computed get countLabel(): string {
//     return `You clicked ${this.count} times`;
//   }
}